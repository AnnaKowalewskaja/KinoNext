import styles from './Profile.module.css';

const Profile = () => {
    return (
        <section className={`${styles.profile} `}>
            Profile
        </section>
    )
}

export default Profile;